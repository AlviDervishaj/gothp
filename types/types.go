package types

// PageData holds data passed to templates.
type PageData struct {
	Title string
	// Add more common fields here, e.g. User, Flash messages, etc.
}

// Other shared type definitions can go in this package.
